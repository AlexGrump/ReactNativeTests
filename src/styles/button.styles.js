import {StyleSheet} from 'react-native';

const buttonStyles = StyleSheet.create({
  button: {
    paddingHorizontal: 20,
    paddingVertical: 10,
    backgroundColor: '#D64E52',
    borderRadius: 6,
    marginVertical: 5,
  },
  buttonLabel: {
    textAlign: 'center',
    color: 'white',
    fontSize: 16,
    lineHeight: 24,
    fontWeight: '600',
  },
});

export default buttonStyles;
